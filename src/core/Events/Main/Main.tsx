import "./Main.sass";

import { Grid, AppContainer, AppHeader } from "uikit";

export const Main = () => {
  return (
    <AppContainer panelHeader={<AppHeader />}>
      <Grid>
        {/*{newsArray.map((item) => {*/}
        {/*  return <EventCard event={item} />;*/}
        {/*})}*/}
      </Grid>
    </AppContainer>
  );
};
