export enum Device {
  Desktop = "desktop",
  Mobile = "mobile",
}

export enum Platform {
  Desktop = "desktop",
  iOS = "ios",
  Android = "android",
}

export enum Modals {
  Bonus = "bonus",
  Currency = "currency",
}

export enum Theme {
  Dark = "dark",
  Light = "light",
}

export enum Mode {
  Default = "default",
  Destroy = "destroy",
  Progress = "progress",
  Primary = "primary",
}
